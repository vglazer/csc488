% ------------------
% Assignment 1, Program b
% Requirements:      A program using arrays but not functions or procedures 
% Description:       This program allocates an array of a large  size, assigns
%                    values to it and uses it in a comparison.  If all goes as
%                    planned, the program outputs "success".
% Expected Output:   "success"
% Key Rules Used:  
%    declaration:    type identifier '[' integer ']'
%       variable:    arrayname '[' expression ']'
%     expression:    expression '<' expression
%      arrayname:    identifier
% ------------------
{
   % variable declaration
   int array[327]

   % variable initialization 
%  array[0] = 2
%  array[326] = -32767

   % array use
%   if array[326] < array[0] then
      put "success"
%  else
%     put "failure"
%  fi
}
